{
  "scripts": {
    "test": "jest",
    "test:ui": "playwright test",
    "test:ui:headed": "playwright test --headed",
    "build": "babel app.jsx --out-file app.js"
  },
  "jest": {
    "//": "e2e/ belongs to Playwright — Jest would try to run those specs and fail on its import.",
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/e2e/"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.28.6",
    "@babel/core": "^7.29.0",
    "@babel/preset-env": "^7.29.2",
    "@babel/preset-react": "^7.28.5",
    "@playwright/test": "^1.62.1",
    "jest": "^29.7.0",
    "recharts": "^2.12.7"
  }
}
